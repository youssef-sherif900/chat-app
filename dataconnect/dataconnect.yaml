specVersion: "v1beta"
serviceId: "chat-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "chat-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
